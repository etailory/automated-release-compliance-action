{
  "name": "governor-os-web",
  "version": "1.0.0",
  "description": "Governor OS Web Platform — unified compliance, roadmap, sprint, and QA management for regulated industries",
  "main": "server.js",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "lint:openapi": "redocly lint openapi.yaml",
    "test": "npm run lint:openapi && node --test server.test.js"
  },
  "dependencies": {
    "express": "^4.19.2",
    "express-rate-limit": "^7.5.0",
    "jose": "^6.2.3"
  },
  "devDependencies": {
    "@redocly/cli": "^1.0.0"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT"
}
